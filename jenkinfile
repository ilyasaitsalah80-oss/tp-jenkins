pipeline {
    agent any

    tools {
        jdk 'JDK21'
        maven 'Maven3'
    }

    stages {
        stage('Build & Tests') {
            steps {
                bat 'mvn clean verify checkstyle:checkstyle pmd:pmd pmd:cpd spotbugs:spotbugs'
            }
        }
    }
}